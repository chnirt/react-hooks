import { shallow } from 'enzyme'
import { render, screen, fireEvent } from '@testing-library/react'

import { DOMCounter } from '../src'

describe('<DOMCounter />', () => {
	test('renders the component', () => {})
})
